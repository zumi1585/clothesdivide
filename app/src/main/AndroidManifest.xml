<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.android.imaizumiriko.clothesdivide">

    <uses-permission  android:name = "android.permission.WRITE_EXTERNAL_STORAGE"  />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".CodeMainActivity" />

        <activity android:name=".CodeListActivity" />

        <activity android:name="._CategorySentakuActivity" />

        <activity android:name=".StartActivity" >
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->
                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>

        <activity android:name=".CodekanseiActivity" />

        <activity android:name=".RegisterActivity" >
        </activity>

        <activity android:name="._KanseisitaCodemiruActivity">
        </activity>
    </application>

</manifest>